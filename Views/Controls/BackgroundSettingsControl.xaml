<UserControl x:Class="cpu_net.Views.Controls.BackgroundSettingsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="500">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Text="背景图片设置" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
        
        <Grid Grid.Row="1" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="BgPathTextBox" Grid.Column="0" IsReadOnly="True" Padding="5,2" FontSize="13" Margin="0,0,10,0"/>
            <Button x:Name="BgBrowseButton" Grid.Column="1" Content="浏览..." Width="80" Height="28" Click="BgBrowseButton_Click"/>
        </Grid>
        
        <Grid Grid.Row="2" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="60"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="背景透明度:" VerticalAlignment="Center" FontSize="14"/>
            <Slider x:Name="OpacitySlider" Grid.Column="1" Minimum="0" Maximum="100" Value="50" 
                    TickFrequency="5" IsSnapToTickEnabled="True" VerticalAlignment="Center" Margin="5,0"
                    ValueChanged="OpacitySlider_ValueChanged"/>
            <TextBlock x:Name="OpacityValueText" Grid.Column="2" Text="50%" VerticalAlignment="Center" FontSize="14" Margin="5,0,0,0"/>
        </Grid>
        
        <TextBlock Grid.Row="3" Text="预览" FontSize="14" FontWeight="Bold" Margin="0,0,0,5"/>
        <Border Grid.Row="4" BorderBrush="#DDD" BorderThickness="1" CornerRadius="4" 
                Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}">
            <Grid>
                <Rectangle>
                    <Rectangle.Fill>
                        <SolidColorBrush Color="White"/>
                    </Rectangle.Fill>
                </Rectangle>
                <Image x:Name="PreviewImage" Stretch="UniformToFill" Opacity="0.5"/>
                <TextBlock Text="CPU_NET" FontSize="24" HorizontalAlignment="Center" VerticalAlignment="Center" 
                           Foreground="Black" Opacity="0.5"/>
            </Grid>
        </Border>
        
        <TextBlock Grid.Row="5" Text="软件图标设置" FontSize="16" FontWeight="Bold" Margin="0,15,0,10"/>
        <Grid Grid.Row="6" Margin="0,0,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="IconPathTextBox" Grid.Column="0" IsReadOnly="True" Padding="5,2" FontSize="13" Margin="0,0,10,0"/>
            <Button x:Name="IconBrowseButton" Grid.Column="1" Content="浏览..." Width="80" Height="28" Click="IconBrowseButton_Click"/>
        </Grid>
    </Grid>
</UserControl>
